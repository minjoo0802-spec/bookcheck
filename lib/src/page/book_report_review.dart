import 'package:flutter/material.dart';

class BookReportReviewPage extends StatelessWidget {
  const BookReportReviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 500,
      width: 300,
    );
  }
}